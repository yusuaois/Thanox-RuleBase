[{"name": "全局自动划卡(带白名单)","description": "by诗里沧海怨怼 | 多应用在开头userIds里指定id序列,如0,15,999","priority": 1,"condition": "systemReady||pkgKilled","actions": ["userIds=new int[]{0};if(systemReady){task.removeAllRecentTasks();}else if(globalVarOf$huakaWhiteApp.contains(pkgName)){return;}for(id:userIds){curPkg=Pkg.newPkg(pkgName,id);if(task.hasTaskFromPackage(curPkg)&&!thanos.activityManager.isPackageRunning(curPkg)){task.removeTasksForPackage(curPkg);}}"]}]