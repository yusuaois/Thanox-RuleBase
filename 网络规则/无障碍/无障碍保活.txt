[{"name": "无障碍保活","description": "by诗里沧海怨怼 | 全局变量内填写服务名即可, 可在进程管理处复制","priority": -1,"condition": "pkgKilled||shortcutLaunched&&shortcutValue=='keepServices'","actions": ["if(pkgKilled){foreach(str:globalVarOf$Accessibility){if(str!=null&&str.startsWith(pkgName)){thanos.profileManager.publishStringFact(1,'keepServices',5000,new String[]{pkgName});return;}}}else{activity.launchProcessForPackage(args[0]);sbSer=new StringBuilder();foreach(str:globalVarOf$Accessibility){if(str!=null&&str!=''){strs=str.split('/\\\\.');if(strs.length>1){sbSer.append(strs[0]).append('/').append(strs[0]).append('.').append(strs[1]);}else{sbSer.append(str);}sbSer.append(':');}}sbSer.setLength(sbSer.length()-1);su.exe('settings put secure enabled_accessibility_services '+sbSer.toString());}"]}]